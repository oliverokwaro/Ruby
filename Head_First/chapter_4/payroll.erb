class Employee
attr_reader :name, :salary

def name=(name)
 if name == ""
 raise "Name cannot be blank!"
 end
  @name = name
end

# there is an error here to be reviewed.
def salary=(salary)
 if salary < 0
 raise "A salary of #{salary} isn't valid"
 end
  @salary = salary
end

def print_pay_stud
 puts "Name is #{name}"
 pay_for_period = (@salary/365.0)*14
 formatted_pay = format("%.2f", pay_for_period)
 puts "Pay for this Period: $#{formatted_pay"}
end

end

amy = Employee.new
amy.name = "Amy Blake"
amy.salary = 50000
